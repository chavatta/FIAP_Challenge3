apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: analytics
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: analytics
  template:
    metadata:
      labels:
        app.kubernetes.io/name: analytics
    spec:
      containers:
        - name: analytics
          image: 633853515534.dkr.ecr.us-east-1.amazonaws.com/togglemaster-analytics:v1.0.0-a6379f784ab3ccf739c1fdd77b3979c89792208e
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
