apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: targeting
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: targeting
  template:
    metadata:
      labels:
        app.kubernetes.io/name: targeting
    spec:
      containers:
        - name: targeting
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
