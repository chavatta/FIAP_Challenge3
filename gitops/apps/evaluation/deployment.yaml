apiVersion: apps/v1
kind: Deployment
metadata:
  name: evaluation
  namespace: togglemaster
  labels:
    app.kubernetes.io/name: evaluation
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: evaluation
  template:
    metadata:
      labels:
        app.kubernetes.io/name: evaluation
    spec:
      containers:
        - name: evaluation
          image: nginx:alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
